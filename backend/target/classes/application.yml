# ===================================
# Canteen Management System - Config
# ===================================

server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/canteen_data?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

logging:
  level:
    com:
      canteen: DEBUG
    org:
      springframework:
        web: INFO

app:
  cors:
    allowed-origins: "*"